{
  "name": "mywebsite-orchestrator",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "seinx-nn-canvas-animation": "^3.3.1"
  }
}
